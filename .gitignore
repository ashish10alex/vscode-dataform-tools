out
dist
node_modules
.vscode-test/
*.vsix
.DS_Store
.excalidraw
user_config.json
src/test/test-workspace/AGENDA.md
webview/initialNodes.ts
src/assets/nodeMetadata.ts

# CLI build artifacts. cli/dist is already covered by the `dist` rule above;
# cli/webview-dist is a per-build copy of dist/ regenerated by `npm run build:cli`.
cli/webview-dist/